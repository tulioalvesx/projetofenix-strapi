version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: newvox
      POSTGRES_USER: newvox
      POSTGRES_PASSWORD: newvox
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
